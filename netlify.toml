# Next.js administra estas rutas. No se crean redirecciones hacia la misma URL,
# porque Netlify las interpreta como un bucle y puede bloquear el registro.
# La proteccion queda en las validaciones del servidor, RLS de Supabase y los
# limites propios de Supabase Auth, sin CAPTCHA visible para el participante.
