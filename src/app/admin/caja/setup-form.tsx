"use client";import{useActionState}from"react";import{createCashierAccount,type SetupState}from"./actions";export function CashierSetupForm(){const[state,action,pending]=useActionState(createCashierAccount,{} as SetupState);return <form action={action} className="reward-admin-form"><label>Nombre del encargado<input name="name" required/></label><label>Correo para ingresar<input name="email" type="email" required/></label><label>Contraseña inicial<input name="password" type="password" minLength={12} required/></label><button disabled={pending}>{pending?"Creando…":"Crear acceso de Caja"}</button>{state.error&&<p className="form-error">{state.error}</p>}{state.success&&<p className="form-success">{state.success}</p>}</form>}
