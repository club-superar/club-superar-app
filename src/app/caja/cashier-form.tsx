"use client";import{useActionState}from"react";import{confirmCashierRedemption,lookupCashierRedemption,type CashierState}from"./actions";
export function CashierForm({defaultCode}:{defaultCode:string}){const initial:CashierState={};const[lookup,lookupAction,looking]=useActionState(lookupCashierRedemption,initial);const[confirmed,confirmAction,confirming]=useActionState(confirmCashierRedemption,initial);return <div className="cashier-flow"><form action={lookupAction} className="reward-admin-form"><label>Código del cliente<input name="code" defaultValue={defaultCode} maxLength={8} autoCapitalize="characters" required/></label><button disabled={looking}>{looking?"Consultando…":"Consultar canje"}</button>{lookup.error&&<p className="form-error">{lookup.error}</p>}</form>{lookup.preview&&<section className="cashier-preview"><p className="eyebrow cyan">REVISÁ ANTES DE CONFIRMAR</p><h2>@{lookup.preview.instagram_username}</h2><strong>{lookup.preview.reward_name}</strong><div><b>{lookup.preview.points} puntos</b><span>${Number(lookup.preview.ars_value).toLocaleString("es-AR")}</span></div><small>Saldo actual: {lookup.preview.balance} puntos</small><form action={confirmAction}><input type="hidden" name="code" value={lookup.preview.code}/><button disabled={confirming}>{confirming?"Confirmando…":"Confirmar y descontar"}</button></form></section>}{confirmed.success&&<p className="cashier-success">✓ {confirmed.success}</p>}{confirmed.error&&<p className="form-error">{confirmed.error}</p>}</div>}
